# packages/shared

Cross-cutting domain contracts and utilities.

Includes:
- JSON Schemas for note templates per language (`schemas/<lang>/note.schema.json`).
- Card-type registry and render specs.
- Shared TypeScript/Python types for API/UI/ingestion.
- Dictionary metadata and constants.
