
  # Extend App into Four Tabs

  This is a code bundle for Extend App into Four Tabs. The original project is available at https://www.figma.com/design/WYesfcS0RqOv1umSf6qeVL/Extend-App-into-Four-Tabs.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  