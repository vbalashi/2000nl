## Public (safe to expose to browser; values are not secrets)
#
# Supabase project URL and anon key. The anon key is intended for client use;
# access control is enforced by Supabase RLS policies.
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
NEXT_PUBLIC_SUPABASE_OTP_LENGTH=6
# Optional: public base URL for audio assets when serving from a different host.
# Example: NEXT_PUBLIC_AUDIO_BASE_URL=https://2000.nuc
# NEXT_PUBLIC_AUDIO_BASE_URL=

## Server-only (DO NOT expose to browser)
#
# Supabase service role / secret key for server-side operations (used by /api/translation).
# Prefer SUPABASE_SECRET_KEY (new name), fallback supported: SUPABASE_SERVICE_ROLE_KEY.
SUPABASE_SECRET_KEY=your-supabase-secret-key
# SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# Optional: if you prefer not to reuse NEXT_PUBLIC_SUPABASE_URL on the server.
# If set, the server will use SUPABASE_URL first.
SUPABASE_URL=https://your-project.supabase.co

# DeepL API key for translations.
DEEPL_API_KEY=your-deepl-api-key

# Optional: override DeepL endpoint (defaults to api-free.deepl.com/v2/translate)
# DEEPL_API_URL=https://api.deepl.com/v2/translate

# OpenAI translation (default translation provider).
# Standard OpenAI:
OPENAI_API_KEY=your-openai-api-key
# Optional override (defaults to https://api.openai.com/v1/chat/completions)
# OPENAI_API_URL=https://api.openai.com/v1/chat/completions
# OPENAI_MODEL=gpt-5.2

# Azure OpenAI (alternative OpenAI backend).
# If AZURE_OPENAI_ENDPOINT + AZURE_OPENAI_API_KEY are set, the app will prefer them for the `openai` provider.
# If AZURE_OPENAI_API_VERSION is set, the app will call the deployments-style endpoint.
# Otherwise it uses the OpenAI-compatible v1 endpoint at `/openai/v1/chat/completions`.
# AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com
# AZURE_OPENAI_API_KEY=your-azure-openai-key
# Optional (recommended for deployments-style endpoint):
# AZURE_OPENAI_API_VERSION=2024-02-15-preview
# AZURE_OPENAI_DEPLOYMENT=your-deployment-name

## Premium Audio / TTS (server-side)
#
# Default audio quality used by /api/tts when the client does not send `quality`.
# Note: the client also needs a build-time default, which is derived from this value
# via apps/ui/next.config.js (NEXT_PUBLIC_AUDIO_QUALITY_DEFAULT).
AUDIO_QUALITY_DEFAULT=free

# When AUDIO_QUALITY_DEFAULT=premium (or user selects premium), choose which backend to use.
PREMIUM_AUDIO_PROVIDER=google
# PREMIUM_AUDIO_PROVIDER=azure

# Google Cloud Text-to-Speech (premium, and also used by the free wrapper).
# Provide either an API key or a service account JSON path.
GOOGLE_TTS_API_KEY=your-google-tts-api-key
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# Azure Speech TTS (premium). Keep each variable on its own line (env_file is line-based).
# AZURE_SPEECH_KEY=your-azure-speech-key
# AZURE_SPEECH_REGION=westeurope
# AZURE_TTS_ENDPOINT=https://westeurope.tts.speech.microsoft.com/cognitiveservices/v1
# AZURE_TTS_VOICE_PREMIUM=nl-NL-FennaNeural
